plugins {
  id 'com.android.library'
  id 'expo-module-gradle-plugin'
}

group = 'app.daymark.backup'
version = '0.1.0'

android {
  namespace "app.daymark.backup"
  defaultConfig {
    versionCode 1
    versionName "0.1.0"
  }
  lintOptions {
    abortOnError false
  }
}
